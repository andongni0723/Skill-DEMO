public enum SkillType
{
    Shoot, AOE, Point
}

public enum Effect
{
    Fizzy, Slowly, Poisoning
}

public enum EffectTarget
{
    Self, TargetEnemy
}

public enum ActionType
{
    Effect, NewObject
}